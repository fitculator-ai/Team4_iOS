import SwiftUI

struct CommunityView: View {
    var body: some View {
        Text("준비중입니다")
            .font(.largeTitle)
            .bold()
            .foregroundStyle(Color.white)
    }
}

#Preview {
    CommunityView()
}
